spring:
  application:
    name: CPNVisualSystem

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://10.126.62.118:3306/slwl?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: org.cpnvisualsystem.entity

server:
  port: 8080

logging:
  level:
    com.baomidou: INFO
    com.alibaba: INFO
