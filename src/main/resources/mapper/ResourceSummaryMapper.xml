<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cpnvisualsystem.mapper.ResourceSummaryMapper">

    <select id="selectByLayer" resultType="org.cpnvisualsystem.entity.ResourceSummary">
        SELECT * FROM resource_summary
        WHERE layer_type = #{layerType}
          AND layer_id = #{layerId}
        LIMIT 1
    </select>

    <select id="selectOverall" resultType="org.cpnvisualsystem.entity.ResourceSummary">
        SELECT * FROM resource_summary
        WHERE layer_type = 'overall'
        LIMIT 1
    </select>

    <select id="selectCarriagesByTrainId" resultType="org.cpnvisualsystem.entity.ResourceSummary">
        SELECT * FROM resource_summary
        WHERE layer_type = 'carriage'
          AND parent_type = 'train'
          AND parent_id = #{trainId}
        ORDER BY layer_id ASC
    </select>

</mapper>
