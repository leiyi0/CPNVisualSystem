<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cpnvisualsystem.mapper.ComputeNodesMapper">
    <select id="selectById" resultType="org.cpnvisualsystem.entity.ComputeNodes">
        SELECT * FROM compute_nodes WHERE id = #{id}
    </select>

    <!-- 根据车厢ID获取设备列表 -->
    <select id="selectDevicesByCarriageId" resultType="org.cpnvisualsystem.entity.ComputeNodes">
        SELECT * FROM compute_nodes WHERE carriage_id = #{carriageId}
    </select>

    <select id="countDevicesByCarriageId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM compute_nodes WHERE carriage_id = #{carriageId}
    </select>

    <select id="countAllDevices" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM compute_nodes
    </select>

    <select id="countOnlineDevices" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM compute_nodes WHERE status = '正常'
    </select>

    <!-- 统计某集群下的设备总数 -->
    <select id="countDevicesByClusterId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM compute_nodes cn
        JOIN carriage_info c ON cn.carriage_id = c.id
        JOIN train_info t ON c.train_id = t.id
        WHERE t.cluster_id = #{clusterId}
    </select>

    <!-- 统计某列车下的设备总数 -->
    <select id="countDevicesByTrainId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM compute_nodes cn
        JOIN carriage_info c ON cn.carriage_id = c.id
        WHERE c.train_id = #{trainId}
    </select>

    <!-- 按集群ID统计设备各状态数量 -->
    <select id="countDevicesByClusterIdGroupByStatus" resultType="java.util.HashMap">
        SELECT COALESCE(cn.status, '未知') AS `status`, COUNT(*) AS `cnt`
        FROM compute_nodes cn
        JOIN carriage_info c ON cn.carriage_id = c.id
        JOIN train_info t ON c.train_id = t.id
        WHERE t.cluster_id = #{clusterId}
        GROUP BY cn.status
    </select>

    <!-- 按列车ID统计设备各状态数量 -->
    <select id="countDevicesByTrainIdGroupByStatus" resultType="java.util.HashMap">
        SELECT COALESCE(cn.status, '未知') AS `status`, COUNT(*) AS `cnt`
        FROM compute_nodes cn
        JOIN carriage_info c ON cn.carriage_id = c.id
        WHERE c.train_id = #{trainId}
        GROUP BY cn.status
    </select>

    <!-- 按车厢ID统计设备各状态数量 -->
    <select id="countDevicesByCarriageIdGroupByStatus" resultType="java.util.HashMap">
        SELECT COALESCE(status, '未知') AS `status`, COUNT(*) AS `cnt`
        FROM compute_nodes
        WHERE carriage_id = #{carriageId}
        GROUP BY status
    </select>
</mapper>